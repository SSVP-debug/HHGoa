{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/s/:id", "destination": "/api/share/:id" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
